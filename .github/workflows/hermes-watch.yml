name: Hermes Watch

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Hermes Check
        run: echo "Hermes Watch is working!"